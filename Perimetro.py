largura = float(input("Digite a largura do retângulo:\n"))
altura = float(input("Digite a altura do retângulo:\n"))

perimetro = (largura * 2) + (altura * 2)

print(f"O perimetro do retângulo é igual à: {perimetro}")

